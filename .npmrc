# Fix Windows MAX_PATH (260 char) limit: move pnpm's virtual store to a short absolute path.
# Without this, pnpm's hash-suffixed package dirs (up to 120 chars) combined with deep
# Android/CMake build paths exceed 260 chars, causing ninja/cmake build failures.
virtual-store-dir=C:\ps
