# Note: If developing on Windows and experiencing MAX_PATH (260 char) limits for local Android builds,
# set 'virtual-store-dir=C:\ps' in your machine's global ~/.npmrc, NOT in this repo file.
# Hardcoded Windows paths in repository .npmrc break Linux deployment environments (Render, Vercel, Docker).

